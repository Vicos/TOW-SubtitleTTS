[project]
name = "subtitle-tts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "av>=18.0.0",
    "edge-tts>=7.2.8",
    "numpy>=2.5.1",
    "rich>=15.0.0",
    "sounddevice>=0.5.5",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
